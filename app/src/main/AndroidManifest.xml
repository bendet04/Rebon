<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.bhinneka.rebon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseTheme.base">
        <activity
            android:name=".activity.MainActivity2"
            android:theme="@style/BaseTheme.base.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity -->
        <activity
            android:name=".activity.CariTiketActivity"
            android:label="@string/activity_titile_cari_tiket"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.DetailWisataActivity"
            android:screenOrientation="portrait"
            android:theme="@style/BaseTheme.base.NoActionBar" />

        <!-- Google MAP API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".activity.TanggalActivity" />
        <activity android:name=".activity.JadwalActivity" />
        <activity android:name=".activity.PilihKursiActivity" />
        <activity android:name=".activity.ReviewPesananActivity" />
        <activity android:name=".activity.PaymentActivity" />
        <activity android:name=".activity.PilihPaymentActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/title_activity_main2" />
        <activity android:name=".activity.DetailPesananActivity"/>
        <activity android:name=".activity.TrackingActivity" />
        <activity android:name=".activity.SewaActivity" />
        <activity android:name=".fragment.SlideShowDialogFragement" />
    </application>

</manifest>